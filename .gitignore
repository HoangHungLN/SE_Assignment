# --- Dependencies (Thư viện) ---
# Bỏ qua thư mục node_modules ở tất cả các cấp (root, client, server)
node_modules/
/node_modules

# --- Environment Variables (Bảo mật) ---
# Bỏ qua các file chứa biến môi trường, key bảo mật
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# --- Client (ReactJS) ---
# Bỏ qua thư mục build khi deploy
client/build/
client/dist/
# Các file cache
.eslintcache

# --- Server (Node.js) ---
# Bỏ qua các file logs
logs/
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pids
*.pid
*.seed
*.pid.lock

# --- IDE & OS (Cấu hình máy cá nhân) ---
# VS Code
.vscode/
# Mac OS
.DS_Store
# Windows
Thumbs.db

# --- Testing ---
coverage/