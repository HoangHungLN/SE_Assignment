# --- 1. Dependencies (Thư viện tải về - QUAN TRỌNG NHẤT) ---
node_modules/
/client/node_modules/
/server/node_modules/

# --- 2. Build Output (File sau khi đóng gói web) ---
build/
dist/

# --- 3. Environment Variables (Biến môi trường/Bảo mật) ---
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# --- 4. Logs (File nhật ký lỗi) ---
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# --- 5. System Files (File rác của Windows/Mac) ---
.DS_Store
Thumbs.db

# --- 6. Editor Directories (Cấu hình riêng của VS Code) ---
.vscode/
.idea/